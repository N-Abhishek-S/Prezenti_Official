# Presenti Web - Frontend Environment

# Optional. Defaults to http://localhost:3001/api/v1 when omitted.
VITE_API_BASE_URL=http://localhost:3001/api/v1

# Optional. Defaults to the local n8n webhook when omitted.
VITE_PS_CHAT_WEBHOOK_URL=http://localhost:5678/webhook/ps-whatsapp
